"""Personas domain package: PersonaCohort + Persona (persistent synthetic respondents).

A cohort is a named, persisted set of persona twins with stable traits and accumulating memory, so
the SAME synthetic respondents can be re-surveyed across waves (longitudinal consistency, U3/U16).
"""
