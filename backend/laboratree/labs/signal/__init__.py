"""Signal Lab — turn any raw mix (PDF, Word, Excel, CSV) into one consolidated master workbook.

The front door of Laboratree: extract everything, then reconcile into a single .xlsx with
segregated sheets plus a data dictionary.
"""
