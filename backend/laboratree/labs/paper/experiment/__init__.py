"""Paper Lab (Experiment) — fetch the paper's data, reconstruct its pipeline, reproduce & explore.

Dedicated folders: fetch/ (auto data-fetch agent + HITL handoff), walkthrough/ (pipeline graph).
"""
