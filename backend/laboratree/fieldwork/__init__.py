"""Fieldwork domain package: Survey / SurveyResponse / Quota persistence models.

The transactional spine for the Field Lab. Pure survey logic lives in ``labs.fieldwork``; this
package holds only the ORM entities (source of truth in Postgres, org-scoped).
"""
